//! Test helper functions for vulnera-deps

/// Assert version constraints are satisfied
pub fn assert_version_satisfies(
    version: &str,
    constraint: &str,
    expected: bool,
) {
    // This would use the actual version constraint parsing logic
    // Placeholder for now
    let _ = (version, constraint, expected);
}

